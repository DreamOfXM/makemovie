{
  "name": "short-drama-studio",
  "private": true,
  "license": "Apache-2.0",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "dev": "pnpm --parallel --filter @studio/api --filter @studio/worker --filter @studio/web dev"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
